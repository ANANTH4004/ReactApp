function Student({ firstName, lastName }) {
  return (
    <div>
      <h1>Student Component -- {firstName}</h1>
      <p>This is a simple student component.-- {lastName}</p>
    </div>
  )
}

export default Student
